<!DOCTYPE html>
<html>
    <head>
        <title>Customer Formular</title>
        <meta charset="UTF-8">
    </head>
    <body>
        <form id="edit_form" action="/customer_save" method="post">
            <input type="hidden" value="${data['customer_form']['key']}" name="key"/>
            <p>
                <label for="label">Company: </label>
                <input type="text" value="${data['customer_form']['label']}" id="label" name="label" required />
            </p>
            <p>
                <label for="contact">Contact: </label>
                <select name="id_contact" id="contact">
                % for key in data['support_employee']:
                    % if key == data['customer_form']['id_contact']:
                        <option value="${key}" selected>${data['support_employee'][key]['first_name']} ${data['support_employee'][key]['last_name']} </option>
                    % else:
                        <option value="${key}">${data['support_employee'][key]['first_name']} ${data['support_employee'][key]['last_name']} </option>
                    % endif
                % endfor
                </select>
            </p>
            <p>
                <label for="location">Location:</label>
                <input type="text" value="${data['customer_form']['location']}" id="location" name="location" required/>
            </p>

            <input type="submit" value="Speichern" />&nbsp;
            <a href="/customer_list">abbrechen</a>
        </form>
    </body>
</html>
