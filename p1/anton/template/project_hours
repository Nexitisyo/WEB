<!DOCTYPE html>
<html>
    <head>
        <title>Project Formular</title>
        <meta charset="UTF-8">
        <link href="/project_form.css" rel="stylesheet"/>
    </head>
    <body>
        <h3>Stunden-Aufwand zuweisen</h3>
        <form id="edit_form" action="/hours_save" method="post">
            <input type="hidden" value="${project_key}" name="key"/>
            % for key in data['project_hours']:
                <input type="hidden" name="id_employee" value="${data['project_hours'][key]['id_employee']}"/>
                ${data['employee'][data['project_hours'][key]['id_employee']]['first_name']}
                ${data['employee'][data['project_hours'][key]['id_employee']]['last_name']}
                <div class="week_box">
                % for value in data['project_hours'][key]['week']:
                    <label for="week_${loop.index}">${loop.index + 1}. Week:</label>
                    <input type="number" id="week_${loop.index + 1}" name="${data['project_hours'][key]['id_employee']}" value="${value}" min="0"/>
                    </br>
                % endfor
                </div>
            % endfor
            </table>
            <input type="submit" value="Speichern"/>&nbsp;
            <a href="/project_list/">Zur&uuml;ck</a>
        </form>
    </body>
</html>
